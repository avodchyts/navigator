<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="db.CityMapper">
    <resultMap id="CityModel" type="db.model.CityModel"/>
    <select id="getCities" resultMap="CityModel">
        select * from city
    </select>
</mapper>